{
  "name": "subschema-loader",
  "version": "2.0.0-0",
  "description": "Subschema Loader",
  "main": "lib/index.js",
  "scripts": {
    "test": "subschema-babel && subschema-mocha",
    "prepublish": "subschema-babel",
    "clean": "rimraf ./lib ./dist"
  },
  "dependencies": {
    "subschema-utils": "^2.0.0-0"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "rimraf": "^2.6.1",
    "subschema-dev-support": "^2.0.0-0"
  }
}
