{
  "name": "subschema-expression",
  "version": "1.7.0",
  "description": "Expressions for subschema",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel -s true src --out-dir dist",
    "test": "yarn run scripts && mocha test/*-test.js",
    "prepublish": "yarn run test"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "lodash": "^4.16.4"
  }
}
