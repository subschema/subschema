/*
var {React, byName, intoWithContext,findNode, TestUtils,expect, byClass,Simulate, click, byTag, byTags, filterProp, byComponent, byComponents}
    = require('../support');

var {templates, ValueManager, Form} = require('Subschema');
var {ButtonTemplate, ListItemTemplate, CollectionCreateTemplate, EditorTemplate} = templates;

var SampleExample = require('../../public/SampleExample.jsx');

describe('SampleExample', function(){


    it('should render Basic', function(){
        var content = require('../../public/samples/Basic');
        var valueManager = new ValueManager();
        var props = {
            schema: content.schema,
            content,
            setup: content.setup,
            file:'Basic',
            sample: 'Basic',
            valueManager
        }
        var sample = intoWithContext(<SampleExample {...props}/>);
        expect(sample).toExist();
    })
});*/
